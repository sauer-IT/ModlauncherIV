<UserControl x:Class="ModlauncherIV.App.RunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="0,4,0,20">

        <ItemsControl ItemsSource="{Binding Rows}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}" Padding="12" Margin="0,0,0,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource Head}" />
                                <TextBlock Text="{Binding Detail}"
                                           Style="{StaticResource Dim}"
                                           Margin="0,3,0,0"
                                           Visibility="{Binding Detail, Converter={StaticResource HasContent}}" />
                            </StackPanel>

                            <TextBlock x:Name="Mark" Grid.Column="1" Text="{Binding Mark}"
                                       Style="{StaticResource Dim}" VerticalAlignment="Center" Margin="12,0,0,0" />
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding State}" Value="Done">
                            <Setter TargetName="Mark" Property="Foreground" Value="{StaticResource Good}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding State}" Value="Failed">
                            <Setter TargetName="Mark" Property="Foreground" Value="{StaticResource Bad}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding State}" Value="Running">
                            <Setter TargetName="Mark" Property="Foreground" Value="{StaticResource Accent}" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Das Protokoll steht offen da und ist nicht versteckt: wenn etwas
             schiefgeht, ist es das Einzige, womit sich nachvollziehen laesst,
             was passiert ist. -->
        <TextBlock Text="PROTOKOLL" Style="{StaticResource Dim}" Margin="0,16,0,6"
                   Visibility="{Binding Log.Count, Converter={StaticResource HasContent}}" />

        <Border Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="1"
                CornerRadius="4" Padding="12" MaxHeight="240"
                Visibility="{Binding Log.Count, Converter={StaticResource HasContent}}">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding Log}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Style="{StaticResource Mono}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

    </StackPanel>
</UserControl>
